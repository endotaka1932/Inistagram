- users.each do |user|
  .relation_user_info
    .relation_user_info_image
      = image_tag user.avatar_image  
    .relation_user_info_name
      %p= user.display_name
      %p= user.last_sign_in_at
